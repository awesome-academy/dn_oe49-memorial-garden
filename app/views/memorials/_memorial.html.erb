<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= avatar_for memorial %>
        <%= memorial.name %>
       <span>
        <%= t("placetime.born_on") %>
        <%= memorial.birth_date.strftime("%d-%m-%Y") %>
       </span>
       <span>
        <%= t("placetime.die_on") %>
        <%= memorial.death_date.strftime("%d-%m-%Y") %>
       </span>
      </h1>
    </section>
  </aside>
</div>
